# Placeholder Spring Boot application properties
# Set active profile to use SQLite for local development
spring.profiles.active=sqlite

spring.datasource.url=jdbc:mysql://turntable.proxy.rlwy.net:53157/railway?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=wtbVcDJQlATQQjylVtYiRrohYWuwpEbi
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Note: this project uses custom JDBC-based DatabaseManager classes for the
# development/demo sqlite profile. JPA/Hibernate has been removed from the
# backend classpath to avoid mapping/DDL conflicts. Keep MySQL settings here
# for production deployments if desired.
